# Entra ID（公司帳號登入）設定。三個變數缺任何一個，登入頁就不會顯示「使用公司帳號登入」按鈕，
# 只會有種子帳號登入表單（公開 demo 模式）。真實值一律放 .env.local（已被 .gitignore 排除），不進版控。
# 對應後端 appsettings.Local.json 的 Entra:TenantId／Entra:ClientId，設定方式見 README。

# Entra 租戶 ID
VITE_ENTRA_TENANT_ID=

# SPA 應用程式註冊的 Client ID（與後端 API 應用程式註冊的 ClientId 不同）
VITE_ENTRA_SPA_CLIENT_ID=

# 後端 API 應用程式註冊 Expose an API 頁面設定的 scope，格式 api://<API ClientId>/<scope 名稱>
VITE_ENTRA_API_SCOPE=
